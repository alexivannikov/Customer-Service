Сервис получения данных покупателей из файла. 
Алгоритм работы сервиса: 
- из файла в формате .json/.txt, который подается на вход контролера, извлекаются данные для построения моделей dto;
- строятся модели dto и передаются на сервисный уровень;
- полученные модели на сервисном уровне мапятся на модели dao с сохранением в БД;
- формируется файл для загрузки с номерами телефонов покупателей, за исключением российских (начинаются с +7),
отсортированных по возрастанию. 
В сервисе реализована Basic-аутентификация. Доступ к методу контролера предоставляется только для администратора. 
Примененный стек:
- Spring (Web MVC, Data JPA, Hibernate, Security);
- Lombok;
- Mapstruct;
- Liquibase;
- Mockito.
